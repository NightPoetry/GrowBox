# 空桩与待接真登记

> 用户决策(2026-06-01):"有些后端还没做好,自然空着很正常。只要记录为文档表示,空桩记载即可。"
> 即:下列空桩**不在 P4 修**,先登记;到对应阶段(P5 做梦睡眠疲劳 / P6 面板接真)再填。
> 详细排查见 `过时文档/可能问题的BUG报告.md`(opusx5,2026-06-01,已归档)。本文件是相对路线图的分类登记,为活文档。

## 一、自然空桩(后端未建,属正常,留待对应阶段接真)

| 命令 / 位置 | 现状 | 接真阶段 |
|---|---|---|
| ~~`cmds.rs dream_start / dream_status`~~ | **已接真(P5,2026-06-01)**:dream_start 取仲裁器 Sleep 档跑 `Memory::sleep` 一轮、报 dreams/discoveries/remaining;dream_status 报 fragment_count/cleared/fatigue。新增 `nap` 命令 = `Memory::nap` | 完成 |
| ~~`cmds.rs get_status` 的 budget_pct / coverage_* / index_density~~ | **已接真(2026-06-01 二级)**:budget_pct=`context_fill_pct`;coverage 三色=`stain_coverage`(Deep深绿/Light浅绿/None灰,无 Red 恒0);index_density=`index_len`/总节点。cache_l1..l3(P3)、fatigue+fragment_count(P5)早接真 | 完成 |
| ~~`cmds.rs get_memory_stats` secondary_indexes~~ | **已接真(精确层阶段4,2026-06-01)**:`total`=`secondary_index_count()`(二级索引锚点数)、`forced_jumps`=`forced_jump_count()`(强制跳转指针数)、fragment_count/cleared(P5)。MemoryViz 面板加"强制跳转"行 | 完成 |
| `cmds.rs get_control_state` | **error_rate_pct / tool_call_total / tool_call_fail 已接真(2026-06-01 二级)**:走唯一分发路径 `Registry::tool_stats()` 原子计数。**`ui_panels` 已接真(活的 IDE Phase 3,2026-06-02)**:前端 `ui_state_changed` 上报各面板真实可见态(含用户手动开关)→ 后端缓存 → 此处上浮;真实翻转 `perceive` 让 agent 感知。仍 0:backup_count(无快照备份机制)、json_*(无 JSON-mode 计量)——诚实置零非谎报 | 余项长期/按需 |
| `cmds.rs set_tools` | 返回 Null(v1 工具集固定,不做禁用) | 设计如此,长期 |
| `cmds.rs confirm_app_exit / suggestion_response` | 返回 Ok,不回流 Agent | 长期 / 按需 |

注:get_status / get_memory_stats 里 P3/P5 已接真的项:`total_nodes`、`pointer_count`(`store.edge_count`)、三级缓存命中率/淘汰、**疲劳值/碎片数(P5)**。coverage_*/index_density/error_rate/tool_call(P6 二级)、secondary_indexes(精确层阶段4)均已接真。当前面板已无假 0(仍诚实置零:backup_count 无快照机制、json_* 无 JSON-mode 计量)。

## 二、虚假成功(不是空桩,是"谎报成功"的真缺陷,登记为债)

> 与 opusx5 已修的 `connect`(加 `probe_llm` 真探测 + 结构化错误码)同根:本该校验 / 落盘却静默放过。
> **这一类不是"自然空着",是已上线代码在说谎。** 按用户决策不在 P4 动,但到「异常告知」写路径接线时一并补,违反铁律「严重异常不得静默」。

| 编号 | 位置 | 缺陷 | 状态 |
|---|---|---|---|
| ~~1.1~~ | `cmds.rs list_models` | 无视 api_base/key 永远返回写死 `[flash,pro]` | **已修(2026-06-01 一级)**:`LlmClient::list_models` 真发 `GET {api_base}/models`,结构化错误码(API_UNREACHABLE/API_BAD_RESPONSE)透传 |
| ~~1.2~~ | `store.rs` 写路径 | 写事务/insert/commit/序列化失败全静默吞 | **已修(2026-06-01 一级)**:全 funnel 进 `WriteHealth` 记账(`write_fault()`),`health_json` 每轮询上浮 `storage_write` Fatal 红警;同时 `Memory::perceive` 让 AI 也感知(见 `内部状态感知.md`) |
| ~~1.3~~ | `state.rs save_settings/save_projects` | `store=None` 时静默 no-op | **已覆盖**:`store=None` 由 `AppState::new` 的 `storage` Fatal 红警 + `perceive("持久化打不开")` 兜住;store 存在时的写失败由 1.2 兜住 |

注:这三条原是"严重异常不静默"铁律的违例,2026-06-01 一级债清理时全部修复。配套新增"内部状态感知层"让 AI 自身也能感知一切失败(决策日志 2026-06-01 ★核心架构★ + `内部状态感知.md`)。
